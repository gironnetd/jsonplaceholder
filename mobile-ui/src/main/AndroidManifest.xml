<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vlabs.jsonplaceholder">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".JsonPlaceholderApplication"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.users.UsersListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.userdetails.UserDetailsActivity"
            android:label="@string/title_activity_users_details"
            android:theme="@style/AppTheme.NoActionBar"/>

        <activity android:name=".ui.photos.PhotosListActivity"
            android:label="@string/title_activity_photos_list"
            android:theme="@style/AppTheme"/>

        <activity android:name=".ui.comments.CommentsListActivity"
            android:label="@string/title_activity_comments_list"
            android:theme="@style/AppTheme"/>
    </application>

</manifest>